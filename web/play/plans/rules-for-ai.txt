Here is our latest set of rules.

For all responses, follow these rules, which are preceded by definitions to clarify wording.

Definitions:
D1. Assigning a new immutable value to a variable is not considered mutation. For example, `var a = (b: 3, c: 5); a = (b: 7, c:9);` is not mutation; it is reassignment.
D2. Fields that are marked `final` cannot be reassigned. (This is intrinsic to Dart, but I am providing it as a reminder.)
D3. If we say something must be "notExtChg", that means it must be "not Externally Changeable". That includes the following:
 - immutable types (such as strings)
 - Stream (even though they are mutable, they are not mutable outside of where they are defined)
 - Observable (same explanation as Stream)
D4. "the disp family" refers to any method/function whose name contains 'disp'. Examples: 'disp()', 'dispCtl'.

Rules section 1:
R1. You MUST never use the `dart:html` library, as it is deprecated.
R2. In all your responses, if you give code, be very specific where to place that code. For example, you could give the full class or function so I can replace it all at once.
R3. Respond in less than 100 words (not including code) or tell me than you can not reasonably answer my prompt in so few words.
R4. All function definitions and function calls MUST be on one line, even if the line is very long. Example: `f(a, b, c, d);` MUST be all on one line, even if the length of the line is more than 700 characters. 
R5. When producing code, you MUST omit any lines in your output which contain only whitespace.
R6. When I provide code and ask you to change it, you MUST keep all comments which are in the provided code. Exception: if the comments are now incorrect due to the changes you have made, you MUST edit them to begin with this: "No Longer valid:".
R7. All indentation MUST be multiples of four spaces. You MUST ignore the Dart standard which suggests multiples of only two spaces.

Rules section 2:
R11. With the exception of the disp family, methods MUST return objects that are notExtChg.
R12. The disp family MUST be called only in the code location 'tabletChildren: [ ... ]', which is in `main()`.
R13. All objects which are global MUST be neither mutable nor reassignable.
R14. The only two global objects (other that functions and classes) MUST be canvWidth and canvHeight.
R15. If a function mutates an object that is an argument/parameter of the function, that function MUST be annotated with the `Mut` metadata to indicate what it is mutating. Note that the `Mut` metadata is not needed if the object being mutated is declared in the function body.
R16. If a function uses the `window` global JS object, it MUST be annotated with the `Eff` metadata and a string description of the side effect.
R17. The `document` global JS object MUST only occur in the following places: (a) the `HTML` class, (b) `main()`.
R18. `draw` functions MUST NOT mutate or reassign any instance variables.
R19. `draw` functions MAY mutate `ctx`.
R20. All Streams MUST be broadcast streams.
R21. `late` keyword MUST not be used.
R22. All public instance variables MUST be notExtChg. The one exception to this rule is the Observable class's `_latestVal`.
R23. All private instance variables (that is, those which begin with an underscore) MUST be either notExtChg or must have the type StreamController.

Does the code follow these updated rules?
