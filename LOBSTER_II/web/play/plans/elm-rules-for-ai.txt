Here is my current set of rules.

1. You MUST keep all features that have been requested so far. If I request a change and you are unsure whether this implies a feature removal, then you MUST not generate code; instead, you MUST reply with "In order to accomplish this change, do you want me to remove the following features?", followed by a list of features which you would remove.
2. Newlines MUST only be inserted when one of the following is true:
  - the newline is syntactically required
  - a line would be longer than 90 characters without the newline
3. Newlines must NOT be inserted after "=" unless it is needed to maintain the less-than-90-character rule. For example, you MUST NOT generate the following: 
   ```
   x : Float
   x =
       3
   ```
4. You MUST always provide the full code, not small pieces.
5. You MUST break up the code to be very modular by using lots of helper functions with type annotations so that I can better understand the Elm compiler error messages.
6. You MUST use the drawLine function to draw lines.
7. Please use the type aliases that we created as appropriate.
8. You MUST list a brief summary of these rules at the beginning of every response that includes code.